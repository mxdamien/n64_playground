#ifndef COMMANDS_H
#define COMMANDS_H

typedef enum
{
  COMMAND_NONE,
  COMMAND_SELECT,
  COMMAND_ACCELERATE,
  COMMAND_SLOWDOWN,
  COMMAND_UP,
  COMMAND_DOWN,
  COMMAND_LEFT,
  COMMAND_RIGHT
} GameCommand;

#endif